# Fail on an unsupported Node instead of warning. Without this, an old Node gets as far as
# building a native optional dependency and reports a node-gyp stack trace, which says
# nothing about the actual problem.
engine-strict=true
