---
description: Brief the team on this session as a 3–4 minute podcast, grounded in the document diff rather than the chat.
---

Run the **session-handoff** skill now.

The people who were not in this session need a briefing they can listen to: what changed in the shared document, which decisions were taken (and which were rejected), what is still open, and where to look.

Do not narrate the conversation. The conversation is the source; the document diff is the ground truth. If there is no diff to show (no git, or the document did not move), offer review mode instead of stopping. If there is no shell, use the `briefing_audio` tool if it is available.

Follow this plugin's `skills/session-handoff/SKILL.md`. If you cannot see it, read `${CLAUDE_PLUGIN_ROOT}/skills/session-handoff/SKILL.md`.
